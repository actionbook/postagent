{
  "private": true,
  "packageManager": "pnpm@10.32.1",
  "scripts": {
    "build": "pnpm -r build",
    "build:core": "cd packages/postagent-core && cargo build --release",
    "dev": "pnpm --filter postagent dev",
    "dev:watch": "cd packages/postagent-core && cargo watch -x build"
  }
}
